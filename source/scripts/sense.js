define('sense', function() {
  return function() {
    var that = this
      , bindings = {};
    
    that.on = function(event, publisher, action) {
      
    }
    that.off = function(event, publisher, action) {
      
    }
  }
});
